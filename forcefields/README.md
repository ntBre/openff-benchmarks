This directory contains force fields in OpenFF `.offxml` format to be
benchmarked by the driver script in the repository root. The table below should
be updated as new files are added.

| Filename | Description |
|----------|-------------|
|          |             |
